# 맥점 봇 매매 규칙 (BTCUSDT · 1d·4h·1h·15m · 양방향)

> 이 문서는 사용자와 합의해 확립한 **신호 규칙의 사람 읽는 원본**이다.
> 코드(strategy.py 등)가 구현, 이 문서가 설명·복구용 기준.
> 최종 갱신: 2026-07-19(다이버전스 스트림 추가). 롱 기준 서술, **숏은 모두 거울(반대)**.

---

## 1. 신호 구조
- **필수 2개(둘 다 충족)** + **나머지 6개 중 ≥3개** → 신호 성립 (2026-07-05: 대각선 제외로 풀 7→6, 문턱 4→3)
- 롱·숏 완전 대칭
- 잠정(봉 형성중, 되돌리면 취소 가능)·확정(봉 마감) **조건 동일, 타이밍만 다름**

## 2. 필수 2 (반드시)
1. **종가 > 선행스팬1** (숏: <)
   - 선행스팬1 = **(전환선+기준선)/2 의 현재 끝점**(shift 없음, 차트 초록선 끝). `ich["senkou1"]`(shift26 과거값) 쓰면 안 됨.
2. **종가 > 20일선** (숏: <)

## 3. 나머지 6 (≥3 필요)
1. **후행스팬 > 26봉 전 종가** (숏: <)
2. **전환선 > 기준선** 골든크로스 (숏: 데드크로스)
3. ~~하락/상승 대각선~~ → **2026-07-05 체크리스트 제외.** 자동 추세선(피벗 앵커)이 가파른 랠리·급반전을 못 따라가는 구조적 한계 → **대각선은 사용자가 직접 작도·확인**(카드 📐 문구가 리마인드).
4. **MACD = 방향**: 골든크로스(MACD선 > 시그널선)면 롱 / 데드크로스면 숏. **0선 돌파 불필요.**
5. **스토캐스틱 = 진입시점**: 50선 돌파.
   - 롱 = **50~80** + %K>%D(GC) + 상향 / 숏 = **20~50** + %K<%D(DC) + 하향
   - **과열(80↑)이면 상방이어도 롱 ❌**(하락 위험), **침체(20↓)면 하방이어도 숏 ❌**(상승 위험)
   - 설정: **키움식 12/5/5** (%K길이12·%K스무딩5·%D스무딩5)
6. **RCI 보조**: **단기선(9) > 중기선(13) 골든크로스 + 단기선 0선 위 + 상향** (숏: 단<중 DC + 단 0선 아래 + 하향)
   - 각도 검증: 0선 위라도 단기선이 꺾여 내려오는 중이면 무효(오실레이터 방향 원칙)
   - 장기선(26)의 0선 돌파는 '롱 유지'일 뿐 진입엔 늦어서 진입 판정엔 안 씀
7. **RCI 그린(장기26) 0선 위** (숏: 아래)
   - 늦지만 추세확인 보조. 이게 같이 켜지면 더 확실(베스트 배지 조건)

> 핵심 역할: **MACD=방향 / 스토=진입시점 / RCI(단9·중13)=보조 / 그린26=확인**

## 4. 오실레이터 일반 원칙 (위치 + 방향)
- 선(0선/50선)의 **위/아래가 '편'을 정하고**, 크로스·각도가 **유효화/무효화**한다.
- 선 위라도 꺾이거나(DC/하향) 반대크로스면 → 롱 ❌ (단, 아직 선 위면 숏도 아님 = 중립)
- 지표 설정: 스토 12/5/5, MACD 12/26/9 EMA, RCI 9/13/26, 일목 9/26/52

## 5. 손절·익절·최종 판단
- **손절**: 직전저점(롱)/직전고점(숏). 불명확하면 ATR(×2) 대체.
- **익절**: **추세선 이탈(돌파) 시** — 직접 보고 판단. 카드 🛑손절 아래 `🏁 익절 = 추세선 이탈 시 (직접 보고 판단)` 라인.
- **⭐ 최우선 철학 = 추세선 (직접 작도·판단)**: **추세선 돌파가 먼저(=델타지역 진입!)**, 그 후에야 선행스팬1·20일선·MACD·스토·RCI 돌파가 의미. 가로 매물대·채널·피보나치도 확인. 카드 체크리스트 직전 📏 블록으로 강조. 자동선이 급등락 못 따라가 대각선은 체크리스트 제외, 사용자 직접.
- 카드 첫 줄 = 방향(🟢롱/🔴숏) 헤더(배지보다 위).
- **카드에서 현재가·손절·익절 표시 제거(2026-08-16)** — 사용자가 직접 보고 판단. 손절 규칙(직전저/고점)은 위 서술대로 유효, 카드에 숫자만 안 띄움.
- **카드 전체 볼드 + 📏멘트 간소화 + 필수 앞 개행(2026-08-16)**: `alert_bot.bold_all()`이 카드 통째 볼드. 📏멘트="[모든 것에 우선]\n추세선 돌파가 먼저(델타지역 진입!) & 가로 매물대·채널·피보나치도 확인". 필수2/2 앞 빈 줄.

## 6. 배지 (2026-07-03 재정의)
- **🎯 막돌파 맥점 (진입 타이밍)**: **3그룹이 모두 최근 2봉 이내에 막 돌파**(2026-07-27 재정의, 창 3→2봉 `fresh_bars=2`, 숏은 하향 거울).
  - **MACD(필수)**: **GC 상태(선>시그널) + 상향(선 상승)** — 숏은 DC 상태(선<시그널) + 하향. (2026-08-15 완화, BTC·XAU 공통: 이전엔 'GC 발생 순간 또는 0선 상향돌파 순간'만이라 크로스가 fresh 창 밖이면 놓침 → 상태+방향으로 변경. 빈도↑.)
  - **스토 %K(필수)**: 50선 상향돌파
  - **RCI (모멘텀 확인, 2026-08-15)**: RCI9 **또는** RCI26 중 **하나라도 0선 위(롱)/아래(숏)면 충족**(상태). MACD 0선 게이트 없음. 핵심 게이트는 스토 50돌파+MACD GC상태고, RCI는 모멘텀 확인만. 최종 진입은 사용자가 추세선 돌파로 선별(봇=후보). (빈도 완화 감수.)
  - fresh(0~3)==3 이면 막돌파. 카드 표시 `n/3`. (이전 "4개 중 3개" → GC포함·RCI택일로 빈도↑.)
  - **동시성 창 `fresh_bars` = env `FRESH_BARS`(기본 2봉)**. **XAU 5m·30m·1h만 3봉(15분)** — 세 트리거가 연속 3봉에 흩어지는 급락(예 08-14 09:15 5m 숏: 스토09:10·MACD09:15·RCI09:20)을 2봉창이 놓쳐서 확장. **XAU 3m·BTC 전부는 2봉 유지**(3분/BTC는 3봉 시 빈도 2배↑ 노이즈). XAU만 유닛 `Environment=FRESH_BARS=3`.
- **⭐ 맥점 완성 (맥점방)**: 막돌파 + 후행·전환 정렬(완성형). 막돌파방 대비 코드박스 없이 ⭐ 배지.
- **⭐ 전조건 정렬**: 나머지 **6/6** (그린26 포함). 추세가 이미 진행 중이라는 뜻 — **추격 주의**로 읽기(진입 타이밍 아님).
- **🔥 강신호**: 나머지 **5/6**
- ~~⚡ 급반전 후보~~: 2026-08-02 카드에서 제거(사용자 요청). fast3 계산은 strategy에 남아있으나 미표시.
- **🌊 N파동 (2026-08-15, 책 큰형님 우선의 법칙)**: 스윙 **A(저)→B(고)→C(저)** higher-low(A<C<B) + C가 A-B의 **피보 되돌림(23.6~78.6%)** + 종가가 **직전고점 B 돌파** → N파동(조정 후 직전고점 돌파). 숏 거울(A>C>B, 직전저점 이탈). **막돌파 카드에 배지로만 표시**(발송 조건 아님). `strategy.nwave_flags`(스윙 pivot 3/3, 최근60봉). 노이즈 있을 수 있어 표시 전용으로 시작.

## 7. 신호 발송 규칙 (노이즈 억제)
- 같은 봉·같은 방향은 **1회만**
- **직전 발송과 같은 방향이 연속이면 억제**(봉을 넘어 유지). **반대 신호(변곡)가 나와야만 재허용 — 시간 리셋 없음**(2026-07-04).
- **라우팅 재개편(2026-07-27): 두 방 다 막돌파(fresh≥3) 기반, 후행·전환 정렬 여부로 분리.**
  - **맥점방(맥점신호 방)** = 막돌파 + **후행스팬(R1)·전환선(R2) 정렬**(완성형). 30일 실측: 15m 15 / 1h 3 / 4h 0.
  - **막돌파방** = 막돌파지만 후행·전환 미정렬(초기 돌파). 30일: 15m 42 / 1h 8 / 4h 1.
  - **비막돌파(느슨한 맥점: 필수2+나머지≥3, fresh<3)는 발송 중단.** 한 신호는 한 방으로만(상호배타).
  - 3분봉은 맥점방 토픽 없음 → **막돌파 전부 막돌파방(3분봉 토픽)** 으로.
  - (참고) 2026-07-25~26 초안: 막돌파=별도방/맥점방=막돌파제외 였으나, 07-27 "맥점=막돌파+후행·전환"으로 재정의.
- **막돌파방은 같은 방향 최초 1회만(2026-07-27)**: `bo_last_dir`로 추적, **반대 방향 막돌파(변곡)가 나와야 같은 방향 재허용**. 같은 방향 연타는 노이즈.
- **맥점방은 같은 방향 억제 해제(2026-08-10)**: `same_dir_blocked` 제거 → 맥점(완성형=막돌파+후행·전환) 조건 충족할 때마다 발송(같은 방향이어도). 같은 봉 중복만 `sent_key`/`alerted_dirs`로 방지. 사용자: 맥점 진입은 계속 알림 원함(막돌파방과 반대 정책).
  - env: `TELEGRAM_CHAT_ID_BO`(막돌파 그룹) + `TELEGRAM_BO_THREAD_1M/_1H/_4H/_1D`(봉별 토픽). 미설정이면 막돌파도 기존 방 규칙(방향전환 1회)으로 폴백.
  - dedup: 막돌파방 `sent_bo=(방향,봉)`, 기존방 `sent_key`/`alerted_dirs`. `last_dir`(변곡 추적)는 어느 방이든 발송 시 갱신 → 기존방은 시장 방향 바뀔 때만 1회.
  - (앞서 같은날 넣었던 "1h/4h/1d 같은방향 우회"는 이 방 분리로 대체됨.)
  - **3분봉 막돌파 전용 봇(2026-07-27, ws_watch_3m.py)**: 3m은 맥점방 없이 막돌파신호 방(토픽 3분봉=14)으로만. 일반 신호 미발송. 토큰 chris15m_bot 재사용. HTF 5m/15m/30m.
  - **카드 표식 구분(2026-07-27)**: **막돌파방** = 회색 코드박스 `<pre>🎯 막돌파 맥점 | DIR | n/3</pre>`(스타일 C, `tg_html`이 `<pre>` 보존). **맥점방** = 코드박스 없이 `⭐ 맥점 완성 — 막돌파 + 후행·전환 정렬` 배지. 3m은 항상 막돌파방이라 🎯 유지. (알림음 구분은 텔레그램 방별 소리 설정.)
- **확정 막돌파 판정 = 에지 아닌 레벨(2026-08-07)**: 확정(봉 마감)에서 `direction_active`(신호 켜짐 상태)+fresh≥3로 판정. 예전엔 "신호 막 켜진 에지 봉"에서만 봐서, fresh가 신호보다 1봉 늦게 완성되면 놓쳤음(예 08-07 22:54 3m 숏: 에지 22:51 fresh 2/3, fresh 3/3은 22:54). 잠정→확정 같은 봉 중복은 `sent_key`/`sent_bo`, 같은 방향 연속은 `last_dir`/`bo_last_dir`로 억제(스팸 없음).
- 잠정은 마감 전 일정 시간(1h=15분/15m=5분) 이상 남았을 때만, 필수2 충족 시에만 발송.
- **확정(마감)도 필수2 충족 시에만 발송(2026-08-14)**: 이전엔 확정 경로에 필수2 가드가 없어, 라이브 형성봉 데이터 순간왜곡으로 spurious fresh가 잡히면 **필수0/2인데도 허위 발송**되던 버그(08-14 09:55 BTC 5m 전부-❌ 숏). 잠정과 동일하게 `must_ok` 가드 추가 + 카드에 `active_dir` 전달(카드 방향=발송 방향) + 미발송시 진단로그(dir/fresh/must). 정상 막돌파(필수2)는 무영향.
- 정확성 > 빈도. 신호 자주 없어도 됨.

## 8. 상위TF 방향 필터
- (2026-08-16 재편) 1h봇: 2h/4h/1d · 30m봇: 1h/2h/4h · **3m·5m봇: 30분/1시간/2시간**(둘 다 동일). (10분봉은 바이낸스 미지원이라 취소.)
- **카드에서 '📊 상위TF 방향' 줄 제거(2026-08-16)** — 🐘 큰형님 정렬 줄이 상위TF 정보 대체.
- **역추세 경고 = 큰형님 정렬 기준(2026-08-18 통일)**: 큰형님 6항목(상위3TF × MACD 0선·스토 50) 중 신호방향 정렬이 **4개 이상이면 추세정렬(경고X)**, 미만이면 ⚠️역추세 경고(다이버전스·피보나치 확인). 차단은 안 함. (이전엔 옛 bias 가중합 기준이라 큰형님 줄과 따로 놀았음 → 통일.)
- **🐘 큰형님 우선의 법칙(2026-08-15, 책 부록 반영)**: 각 봉의 **앞선 3개 상위TF**(상위TF 방향 줄과 동일: 5m→15/30/60분 등) 각각 **MACD 0선·스토 50선**이 신호 방향과 정렬됐는지 **TF별로** 카드에 표시(2026-08-18: MACD를 GC/DC→**0선 기준**으로 변경, 책 p190 "상위TF MACD 0선 돌파"). 롱=MACD 0선 위 & 스토 50 위 / 숏 거울. `🐘 큰형님 정렬(MACD·스토) — 15분 ✅·✅ / 30분 ❌·❌ / 1시간 ❌·❌`(각 TF `MACD·스토` 순, ✅=정렬). 표시 전용(발송 억제 아님). 롱=MACD GC & 스토50위 / 숏 거울. 책: 큰형님 강한 방향+MACD 0선돌파·스토50돌파 시 하위봉 맥점 진입 고확률.

## 9. 다이버전스 (2026-07-19 추가 → **같은 날 발송 중단**) — 맥점과 **완전 별개 스트림**
> ⛔ **2026-07-19 발송 OFF (`divergence.py` `ENABLED=False`).** 이유: 추세장에서 노이즈 과다.
> 단순 피벗 규칙이 "진짜 다이버전스"와 "추세 하강/상승 중 미세 스윙"을 구분 못 함
> (예: 15m 17:45 하락 다이버전스 오탐 — 고점 64,722→64,724 +2p를 HH로, 하락추세 MACD선의
> 아무 두 점을 LH로 잡음. 실제론 가격·MACD 둘 다 하락추세일 뿐). 사용자: "그냥 노이즈, 주지마."
> 아래 규칙/코드는 보존, 재개하려면 `ENABLED=True`. (개선안: 최소 유의미 이동폭 필터·추세 컨텍스트 필요.)

- **지표 기준 = MACD선(파란선, `macd()[0]`)**. (RSI 없어서 대체. 처음엔 시그널선(주황)으로 했다가 2026-07-19 사용자 정정 → 파란선=MACD선으로 변경.) 가격 스윙 고/저점 2개를 이어, 그 시점 MACD선 값과 비교.
- **스윙 파라미터**: 피벗1(좌우 1봉, 확정 지연 최소화) + **두 비교 스윙 최소 간격 10봉**(`min_gap=10`, 2026-07-19 추가 — 너무 가까운 스윙끼리 비교 금지, 전 시간봉 공통).
- **맥점 신호와 절대 섞지 않음(별도 메시지).** 기본은 **각 봉의 기존 토픽**에 다이버전스 카드를 별도 메시지로 발송(카드 헤더 🔀로 구분). 전용 토픽을 쓰고 싶으면 `TELEGRAM_DIV_THREAD_1M/_1H/_4H/_1D`를 설정 → 없으면 기존 토픽(`TELEGRAM_THREAD_ID*`)으로 폴백.
- **발송 종류 = 일반 + 히든 (과장 제외).** 각각 하락·상승.
  - **일반(추세 반전)**: 하락 = 가격 고점↑(HH) & MACD선 고점↓(LH) / 상승 = 가격 저점↓(LL) & MACD선 저점↑(HL)
  - **히든(추세 지속)**: 하락 = 가격 고점↓(LH) & MACD선 고점↑(HH) / 상승 = 가격 저점↑(HL) & MACD선 저점↓(LL)
  - 과장(가격 ≈일정)은 감지 코드엔 있으나 발송 안 함(`detect` kinds 기본 = 일반·히든).
- **발송 시점 = 봉 마감 확정만**(잠정 없음). 스윙은 오른쪽 1봉이 지나야 확정 → 15m 기준 저점/고점 후 약 15분 뒤 발송(피벗1). 2번째 피벗이 막 확정된 봉에서 1회(에지 트리거, `max_age=2`). 같은 (종류,방향,2번째피벗)은 중복 발송 안 함.
- 참고용 판독 신호 — 맥점처럼 진입 조건 아님. 카드에 면책 문구.
- 구현: `divergence.py`(detect/card/check), 각 봇 `is_closed` 분기에서 `divergence.check(...)` 호출, `LiveState.sent_div`로 dedup.

## 9.5 30분봉 스토캐스틱 과열/침체 환경 알림 (2026-08-10 추가)
- **목적**: 상위(30분봉) 스토캐스틱이 과열/침체면 하위봉(3·5분) 진입 방향을 편향시키는 **환경 필터**.
  - **과열(%K ≥ 80)** → 곧 하락 조정 가능성 ↑ → **3·5분봉 숏 신호 나오면 숏 유리**.
  - **침체(%K ≤ 20)** → 곧 반등 가능성 ↑ → **3·5분봉 롱 신호 나오면 롱 유리**.
- **판정 = 키움식 12/5/5 %K**. **잠정(형성봉 %K)+확정(마감 %K) 병행**(2026-08-13 잠정 추가).
- **발동 = 존 '진입' 1회 + '해소'(존 이탈) 1회**(2026-08-10 해소 추가).
  - 진입: %K 80 상향돌파(과열)/20 하향돌파(침체). 존 안에선 반복 억제.
  - 해소: %K가 80 아래로(과열 해소)/20 위로(침체 해소) 복귀 → 🥵➡️😌 / 🥶➡️😌 카드 1회. "과열 식음=하락 실제 진행 가능 / 침체 벗어남=반등 실제 진행 가능".
  - 봇 시작 시 기존 상태로는 재알림 안 함(load_base에서 존 초기화).
- **잠정(미확정) 알림 (2026-08-13)**: 봉 형성중 %K가 존 진입/해소 조건을 만족하면 **마감 30분 안 기다리고 즉시 ⏳[잠정] 카드 1회**(마감 5분 전까지, `PROV_MIN_MINS_LEFT`·`RECOMPUTE_MIN_SEC` 게이트). **봉당 1회**(`prov_stoch_bar`/`prov_stoch_events`), 마지막 '확정' 존 대비로 판정.
  - **마감 정정**: 마감 때 잠정 이벤트가 확정되면 기존 확정 카드(🥶/🥵/➡️😌) 발송, **되돌려서 미확정이면 ❌[정정] 취소 카드** 발송(`fmt_stoch_cancel`). → 리페인트 투명 처리.
  - 확정 카드 조건·문구는 종전과 동일, 타이밍만 앞당김(잠정=형성봉, 확정=마감).
- **종목 라벨 (2026-08-13)**: 메시지 헤더에 `[BTC]`/`[XAU]`(`SYM_TAG`) 표기 — BTC·XAU 30m 봇이 **같은 봇계정 공유**라 텍스트 구분 필요. 예: `🥵 [XAU] 30분봉 스토캐스틱 과열`.
- **발송처**: BTC=맥점방 30m 토픽(559)/막돌파 85, **XAU=XAU 30m 토픽(622)/막돌파 207**. ⚠️XAU 토픽 override는 `/etc/maekjeom-bot-xau.env`+유닛 드롭인으로(inline `Environment=`는 EnvironmentFile에 덮여 무효 — 2026-08-13 버그수정, CLAUDE.md 참고).
- 맥점/막돌파/다이버전스와 별개 메시지(🥵/🥶 헤더). 진입 신호 아님, 환경 참고용(면책 문구).
- 구현: `ws_watch_30m.py` `stoch_zone_of()`·`fmt_stoch_regime(provisional=)`·`fmt_stoch_cancel()`. 확정=`is_closed` 분기(`stoch_zone`), 잠정=형성중 분기(`prov_stoch_*`, 맥점 `if not d` 앞에 배치).

## 10. 운영
- 코드: GitHub `ipobrief/maekjeom-bot` (main)
- 봇(2026-08-04, 3m·5m·30m·1h): Oracle Cloud(도쿄) systemd — `maekjeom-bot`(1h) / `maekjeom-bot-5m`(5m, chris15m_bot) / `maekjeom-bot-30m`(30m, chris4h_bot) / `maekjeom-bot-3m`(3m, chris1d_bot). 15m·4h·1d 봇 중단. 각 봉 맥점방/막돌파방 풀 라우팅.
- 데이터: Binance(data-api.binance.vision)
- 종목: **BTCUSDT** + **XAUUSDT(금 무기한선물, 2026-08-12 추가)**. 알트 미감시.
  - XAU는 **BTC와 완전 동일한 규칙**(위 1~9.5 그대로). 코드 공유, `SYMBOL`/`WS_BASE` env로 종목만 교체.
  - XAU 데이터·웹소켓은 **선물 전용**(fapi + `wss://fstream.binance.com`; 금은 현물 페어 없음).
  - 발송: 기존 맥점신호/막돌파신호 그룹에 **XAU 전용 토픽** 신설(BTC와 섞이지 않음). systemd `maekjeom-bot-xau-{3m,5m,30m,1h}`.
  - ⚠️ 금은 주말·연말 등 휴장 때 거래정지/데이터 갭 가능(BTC 24/7과 다름).
  - **XAU 주말 발송 억제(2026-08-15)**: KST 토06:00~월08:00(COMEX 금 휴장) XAU 신호 OFF — 주말 유동성 평일 1/10·횡보라 매매 부적합. BTC는 24/7 유지. (`WEEKEND_OFF=1` env + `alert_bot.weekend_muted()`.)
